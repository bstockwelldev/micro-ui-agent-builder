{
  "name": "micro-ui-agent-builder",
  "version": "0.1.0",
  "private": true,
  "description": "Prototype agentic flow studio (Next.js + shadcn + AI SDK 6)",
  "scripts": {
    "dev": "pnpm --filter web dev",
    "build": "pnpm --filter shared build && pnpm --filter web build",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm --filter @repo/shared build && pnpm -r typecheck",
    "test": "pnpm --filter shared test && pnpm --filter web test",
    "stitch:download-screen": "node scripts/stitch-download-screen.mjs"
  },
  "engines": {
    "node": ">=20",
    "pnpm": ">=9"
  },
  "packageManager": "pnpm@9.15.4",
  "devDependencies": {
    "@google/stitch-sdk": "^0.0.3"
  }
}
